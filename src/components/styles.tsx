import { createGlobalStyle } from 'styled-components';

export const TerminalStyles = createGlobalStyle`
  .home-terminal-text {
    font-family: 'JetBrains Mono', monospace;
    /* rest of your styles */
  }
  
  .welcome-message {
    /* styles */
  }
`; 